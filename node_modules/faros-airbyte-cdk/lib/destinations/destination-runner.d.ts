import { Command } from 'commander';
import { AirbyteLogger } from '../logger';
import { AirbyteConfig } from '../protocol';
import { Runner } from '../runner';
import { AirbyteDestination } from './destination';
export declare class AirbyteDestinationRunner<Config extends AirbyteConfig> extends Runner {
    protected readonly logger: AirbyteLogger;
    protected readonly destination: AirbyteDestination<Config>;
    constructor(logger: AirbyteLogger, destination: AirbyteDestination<Config>);
    mainCommand(): Command;
    specCommand(): Command;
    checkCommand(): Command;
    writeCommand(): Command;
    specPrettyCommand(): Command;
    airbyteLocalCLIWizardCommand(): Command;
    private loadConfig;
}
